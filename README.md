# Dogkeeper

A CLI game for managing your dogs, built with Python and Rich.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Usage
```bash
./run.sh                # Linux/Mac
python3 dogkeeper.py    # Windows
```
